# The :dev-sdlc content-dev variant bundles a self-hosted Gitea, a Gitea Actions
# runner, and a local container registry (git.dockerlabs.xyz / registry.dockerlabs.xyz)
# used by Lab 3 — Securing Your CI Pipeline.
include:
  - oci://dockersamples/labspace-content-dev:dev-sdlc
  - ./compose.override.yaml
