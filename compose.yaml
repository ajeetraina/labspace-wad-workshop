include:
  # SDLC variant: adds Gitea, a container registry, k3s and Traefik.
  # Lab 3 needs a real CI pipeline, so the base variant is not enough.
  - oci://dockersamples/labspace-content-dev:latest-sdlc
  - ./compose.override.yaml
