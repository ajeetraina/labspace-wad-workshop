services:
  configurator:
    environment:
      # The Product Catalog app, with the agent-generated Dockerfile committed.
      # See README.md — you must create this fork before first boot.
      PROJECT_CLONE_URL: https://github.com/ajeetraina/catalog-service-wad

  workspace:
    image: dockersamples/labspace-workspace-node
